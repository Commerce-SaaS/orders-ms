export const SECTOR_PATTERNS = {
  CREATE: 'sector.create',
  FIND_ALL: 'sector.find_all',
  FIND_ONE: 'sector.find_one',
  UPDATE: 'sector.update',
  SOFT_DELETE: 'sector.soft_delete',
  RESTORE: 'sector.restore',
} as const;
